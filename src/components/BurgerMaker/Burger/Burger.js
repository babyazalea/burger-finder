import React from "react";

import { Container } from "react-bootstrap";
import "./Burger.scss";

const Burger = (props) => (
  <Container className="burger">
    <div className="lid-bun"></div>
  </Container>
);

export default Burger;
